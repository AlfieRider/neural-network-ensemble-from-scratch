# neural-network-ensemble-from-scratch
A neural network ensemble built from scratch (i.e. no ML frameworks, etc) using soft-voting across 4 independently trained models to classify handwritten digits.
